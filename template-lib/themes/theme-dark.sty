% ============================================================================
% theme-dark.sty — Dark Mode Theme (Catppuccin Macchiato-like)
% ============================================================================
% Eye-friendly dark theme for low-light presentations.
% ============================================================================

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{theme-dark}[2026/05/23 Dark Beamer Theme]

% ── Color Palette ───────────────────────────────────────────────────────────
\definecolor{TLprimary}{HTML}{8aadf4}      % Soft blue — primary
\definecolor{TLprimaryDark}{HTML}{5b6078}  % Muted purple-gray
\definecolor{TLprimaryLight}{HTML}{363a4f} % Dark surface
\definecolor{TLprimaryTint}{HTML}{24273a}  % Darker surface
\definecolor{TLaccent}{HTML}{eed49f}       % Warm gold — accent
\definecolor{TLaccentLight}{HTML}{32301f}  % Dark gold bg
\definecolor{TLink}{HTML}{cad3f5}          % Light lavender — text
\definecolor{TLinkSoft}{HTML}{a5adcb}      % Muted text
\definecolor{TLsurface}{HTML}{1e2030}      % Deep background
\definecolor{TLpos}{HTML}{a6da95}          % Soft green
\definecolor{TLneg}{HTML}{ed8796}          % Soft red

% ── Semantic Aliases (dark theme: canvas = surface) ────────────────────────
\definecolor{TLcanvas}{named}{TLsurface}
\definecolor{TLblockbody}{named}{TLsurface}

% ── Mark as dark ────────────────────────────────────────────────────────────
% Opts into theme-base's centralized dark chrome (also fixes title-page and
% table-header contrast). The explicit overrides below remain as-is and are
% now redundant duplicates of what theme-base sets — kept for clarity.
\TLdarktrue

% ── Shared Boilerplate (light defaults) ─────────────────────────────────────
\input{template-lib/themes/theme-base.sty}

% ── Dark-theme Overrides ───────────────────────────────────────────────────
% theme-base provides light-theme defaults; dark themes override the divergent
% beamer-color assignments that differ from the light wiring.
\setbeamercolor{frametitle}{bg=TLprimaryLight,fg=TLprimary}
\setbeamercolor{frametitle right}{bg=TLprimaryTint}
\setbeamercolor{palette primary}{fg=TLink,bg=TLprimaryLight}
\setbeamercolor{block body alerted}{bg=TLsurface}
\setbeamercolor{block body example}{bg=TLsurface}
\renewcommand{\TLtitlebg}{\setbeamercolor{background canvas}{bg=TLprimaryTint}}
\renewcommand{\TLresetbg}{\setbeamercolor{background canvas}{bg=TLsurface}}

% ── Theme-specific Footer ───────────────────────────────────────────────────
\renewcommand{\TLfootleft}{Dark Presentation}
